<?php
// ordinary form-submit by php
if(isset($_POST['submitForm1'])){
    var_dump($_POST);
    // manage field-values and save DB or as you want
}

if($_REQUEST['act']=='submitForm2'){
    var_dump($_REQUEST);
    // manage field-values and save DB or as you want
}

if($_REQUEST['act']=='submitForm3'){
    var_dump($_REQUEST);
    // manage field-values and save DB or as you want
}

if($_REQUEST['act']=='submitForm4'){
    var_dump($_REQUEST);
    // manage field-values and save DB or as you want
}
if($_REQUEST['act']=='submitForm5'){
    var_dump($_REQUEST);
    // manage field-values and save DB or as you want
}
?>